import { SainitizeHtmlPipe } from './sainitize-html-pipe';

describe('SainitizeHtmlPipe', () => {
  it('should create an instance', () => {
    expect(new SainitizeHtmlPipe()).toBeTruthy();
  });
});
